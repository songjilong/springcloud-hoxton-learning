server:
  port: 80
eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka

ribbon:
  ConnectTimeout: 5000
  ReadTimeout: 5000

# 在feign中开启hystrix
feign:
  hystrix:
    enabled: true